version: "3.9"

services:
  app:
    build: .
    container_name: sid3d-app
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload
volumes:
  sid3d_data:
